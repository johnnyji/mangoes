<%= form_for(@review, url: movie_reviews_path(@movie), html: { class: "review-form" }) do |f| %>
  <%= f.object.errors[:text].first %>
  <%= f.text_area :text, placeholder: "Your review" %><br>

  <%= f.object.errors[:text].first %>
  <%= f.number_field :rating_out_of_ten, placeholder: "Your rating out of 10" %><br>

  <%= f.submit %>
<% end %>