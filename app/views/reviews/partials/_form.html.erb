<%= form_for(@movie.reviews.build, url: new_movie_review(@movie)) do |f| %>

  <%= @review.errors[:text].first %>
  <%= f.text_area :text, placeholder: "Your review" %>

  <%= @review.errors[:text].first %>
  <%= f.number_field :rating_out_of_ten, placeholder: "Your rating out of 10" %>

  <%= f.submit %>
<% end %>